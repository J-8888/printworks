# Asset Manifest

## Icons
| File | Source | Purpose |
|---|---|---|
| `src/icons/index.tsx` | hand-authored SVG components | All UI icons (cube, clipboard, pound, printer, box, search, chevrons, plus, x, trend, bell, trash) |

## Favicon
| File | Source | Purpose |
|---|---|---|
| `public/favicon.svg` | hand-authored SVG | Browser tab icon — hexagonal cube mark in green on dark |

## Notes
- No raster hero images required — this is a data dashboard with no pictorial content
- All visual elements are CSS/SVG
- Status badge colours defined in CSS @theme tokens
